<Window x:Class="Kanban.EliminarUsuariWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Eliminar Usuari" Height="185" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <!-- EliminarUsuariWindow: seleccionar un usuari del grup per eliminar -->
    <!-- Aquesta finestra nomes retorna el nom; el MainWindow demana confirmacio i elimina a la BDD -->

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Text informatiu -->
        <TextBlock Grid.Row="0" Text="Selecciona l'usuari a eliminar de la BD:"
                   FontWeight="SemiBold" Margin="0 0 0 10"/>

        <!-- ComboBox amb usuaris del grup -->
        <ComboBox x:Name="cmbUsuaris" Grid.Row="1" Width="250" Height="28"/>

        <!-- Botons: eliminar o cancelar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 20 0 0">
            <Button Content="Eliminar" Width="90" Height="30" Margin="0 0 10 0"
                    Background="#F44336" Foreground="White"
                    Click="BtnEliminar_Click"/>
            <Button Content="Cancelar" Width="90" Height="30"
                    Click="BtnCancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>
