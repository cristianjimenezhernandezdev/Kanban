<Window x:Class="Kanban.DesvincularParticipantWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Desvincular Participant" Height="190" Width="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
           
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Selecciona el participant a desvincular:"
                   FontWeight="SemiBold" Margin="0 0 0 10"/>

        <ComboBox x:Name="cmbParticipants" Grid.Row="1" Width="250" Height="28"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 20 0 0">
            <Button Content="Desvincular" Width="90" Height="30" Margin="0 0 10 0"
                    Background="#FF9800" Foreground="White"
                    Click="BtnDesvincular_Click"/>
            <Button Content="Cancelar" Width="90" Height="30"
                    Click="BtnCancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>
