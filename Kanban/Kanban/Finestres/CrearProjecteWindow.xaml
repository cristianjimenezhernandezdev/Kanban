<Window x:Class="Kanban.CrearProjecteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Crear Projecte"
        Height="250"
        Width="350">

    <!-- CrearProjecteWindow: formulari per crear un nou projecte/sprint -->
    <!-- Camps: titol + data fi + boto crear -->

    <Grid Margin="20">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titol del projecte -->
        <TextBlock Text="Titol del projecte"/>
        <TextBox x:Name="txtTitol"
                 Margin="0,20,0,10"/>

        <!-- Data fi del projecte -->
        <TextBlock Text="Data final"
                   Grid.Row="1"/>
        <DatePicker x:Name="dpDataFi"
                    Grid.Row="1"
                    Margin="0,20,0,0"/>

        <!-- Boto per executar la creacio (va a BtnCrear_Click) -->
        <Button Grid.Row="2" Style="{StaticResource BotoBase}"
                Content="Crear"
                Height="35" Background="LightGray" Foreground="Black"
                Width="100"
                HorizontalAlignment="Center"
                Margin="0,20,0,0"
                Click="BtnCrear_Click"/>
    </Grid>
</Window>

